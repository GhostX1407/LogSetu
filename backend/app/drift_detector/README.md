# Drift Detector
Tracks parse success rate / field-population rate per source over time.
Flags when a known source's format silently changes, auto-triggers the
AI Integrator to propose an updated mapping.
