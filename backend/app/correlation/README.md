# Correlation Engine (toy version)
Links related normalized events across 2+ sources sharing IP/user/hostname
within a time window (e.g. firewall block + IAM failed login). Tags linked
incidents with a MITRE ATT&CK technique ID from a small lookup table.
