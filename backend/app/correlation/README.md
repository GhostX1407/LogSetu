# Correlation Engine
Links related normalized events across 2+ sources sharing IP/user/hostname
within a configurable temporal window (e.g. firewall block + IAM failed login).
Tags linked incidents with MITRE ATT&CK technique IDs.
